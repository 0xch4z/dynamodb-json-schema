{
  "name": "dynamodb-json-schema",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf dist",
    "prebuild": "yarn clean",
    "build": "tsc -p ."
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.12",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/json-schema": "^7.0.3"
  }
}
